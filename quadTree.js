class QTree {

  constructor(boundary, n){
    this.boundary = boundary;
    this.capacity = n;
    this.elements = [];
    this.isParent = false;
  }

  subdivide(){
    this.northeast = new QTree()
  }

  insert(element){
    if(this.elements.length)
  }
}
